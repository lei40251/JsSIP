# SDK 项目开发规范

这是一个浏览器侧 SIP + WebRTC SDK 项目。核心目标是保证 SDK API 清晰、通话流程稳定、Demo 具备接入教学价值。

## 核心原则

* 保持简单（KISS），不为未来需求设计（YAGNI）。
* 优先解决当前问题，避免过度设计和无意义抽象。
* 优先修改现有代码，而不是新增大量文件或目录层级。
* 优先保证代码可维护、可调试、调用链清晰。
* SDK 的核心逻辑应该容易定位，重要逻辑不要隐藏在多层包装之后。

---

## 指令优先级

当规则之间出现冲突时，按以下优先级处理：

1. 用户本次明确要求。
2. 不破坏公开 API 和历史行为。
3. 保证通话、媒体和浏览器兼容性稳定。
4. 最小修改，避免无关重构。
5. Demo 的 SDK 教学价值。
6. 代码风格、文档完整性和 CHANGELOG 规范。

如果无法同时满足高优先级规则和低优先级规则，应优先满足高优先级规则，并在最终说明中解释取舍。

---

## 修改原则

进行任何修改时，应优先遵循以下顺序：

1. 在现有函数内修改。
2. 现有函数无法清晰承载时，再新增私有函数。
3. 同一模块内无法表达时，再新增文件。
4. 确有必要时，再新增目录或抽象。

Bug 修复优先采用最小修改原则：

* 尽量修改已有逻辑。
* 不改变正常流程。
* 不影响已有 API 和历史行为。
* 不为了修一个 Bug 重构整个模块。
* 不为了局部需求引入全局机制。

---

## 修改前阅读上下文

修改代码前，应先阅读必要上下文：

* 当前函数。
* 调用方。
* 被调用方。
* 同文件相关逻辑。
* 相关测试和 Demo 示例。

不要只根据局部代码生成实现。保持已有代码风格、命名习惯、错误处理方式和日志风格。

---

## 仓库结构

* `lib/`：SDK 源码。
* `lib/JsSIP.js`：SDK 对外导出入口。
* `lib/UA.js`、`lib/RTCSession.js`：SIP、注册、通话和会话核心逻辑。
* `lib/MediaEffectsComposer/`：混流、虚拟背景、水印、渲染链路等媒体特效能力。
* `lib/AiNoiseSuppression/`：AI 降噪能力。
* `lib/BFCP/`、`lib/RTCSession/BFCPChannel.js`：BFCP 相关能力。
* `demo/base-js/`：主要 SDK 接入 Demo，优先作为客户接入示例。
* `samples/`：补充示例、实验代码和第三方资源，修改前先确认是否属于当前任务。
* `docs/`：接入文档、API 文档和媒体能力说明。
* `test/`：单元测试和专项测试。
* `dist/`：构建产物。
* `release/`：本地发布压缩包输出。

---

## 架构与文件组织

* 不要为了架构而架构，不要为了使用设计模式而使用设计模式。
* 不要创建没有实际价值的 Manager、Factory、Adapter、Repository、Strategy 等抽象。
* 如果已有模块边界需要兼容层、渲染器、媒体管线或适配层，应沿用现有结构，不要强行重命名或重构。
* 相同逻辑出现 3 次以上再考虑抽象。
* 相关逻辑尽量放在一起，小型功能允许放在同一个文件中。
* 新增文件前先确认是否能放入已有模块；新增目录层级要有明确收益。
* 除非任务明确要求，不要批量重命名变量、批量移动文件、批量拆分类、全局格式化、大规模代码整理、重排 import 或修改既有代码风格。
* 任务应聚焦于当前需求，避免扩大修改范围。

---

## SDK 开发规范

* 优先保证 API 易理解、调用流程清晰、调试方便。
* 尽量减少依赖，不引入无实际价值的库或框架。
* 对外 API、事件名、事件 payload、参数含义和默认行为不要随意变更。
* 修改公开 API 时，应同步检查 `.d.ts`、文档、Demo 和测试。
* 历史 API 和兼容写法应尽量保留；确需移除或改变行为时，必须明确说明影响范围。
* 除非任务明确要求，否则不要修改默认参数、默认超时时间、默认重试次数、默认事件触发顺序、Promise resolve/reject 时机、Error 类型和返回值结构。
* WebRTC/SIP 相关改动要保守，不要改变已有时序。
* 媒体能力失败时，优先保证通话继续，必要时降级或回退原始音视频流。
* 注意浏览器兼容，重点关注 Chrome、Edge、Safari、Firefox、Android Chrome、Android WebView、微信浏览器和鸿蒙浏览器。

---

## WebRTC 高风险区域

以下逻辑属于高风险区域，修改时必须充分阅读上下文、保持时序稳定，并优先补充或运行对应测试：

* SDP 生成与修改。
* ICE Candidate 和 ICE Restart。
* DTLS、SRTP 和媒体安全协商。
* `RTCRtpSender.replaceTrack`。
* `addTrack`、`removeTrack` 和 track 生命周期。
* `transceiver.direction`。
* renegotiation、re-INVITE、hold/unhold、early media。
* BYE、CANCEL、REFER、183/200 OK 等信令时序。
* BFCP 和 DataChannel。
* 屏幕共享、设备切换、mute/unmute。
* `getStats` 和媒体质量统计。

---

## 构建与测试

推荐使用 `package.json` 中的 npm scripts：

* `npm run lint`
* `npm run test`
* `npm run build`
* `npm run build:min`
* `npm run build:standard`
* `npm run release`

注意事项：

* Demo 和部分 samples 加载的是 `dist/CRTC.min.js` 或其他构建产物。
* 修改 `lib/` 后，如果需要验证 Demo 行为，应先重新构建。
* `gulp devel` 会根据 `lib/Grammar.pegjs` 更新 `lib/Grammar.js`，只在确实修改语法时运行。
* 测试范围应与改动风险匹配；修改媒体、SDP、通话状态、兼容逻辑时应补充或运行对应专项测试。
* 修改公开 API 时，建议检查顺序为：代码、`.d.ts`、测试、Demo、文档、CHANGELOG。
* 修改媒体流程时，至少考虑音频、视频、屏幕共享、设备切换、挂断、重呼等路径是否受影响。

---

## 验证选择

根据改动范围选择验证方式：

* 只修改文档、AGENTS.md 或注释：检查 `git diff`，通常无需运行完整测试。
* 修改普通 SDK 源码：优先运行 `npm run lint` 和相关测试。
* 修改通话、SDP、WebRTC、媒体链路、设备切换或兼容逻辑：优先运行 `npm run test`。
* 修改构建、打包、发布或 `dist/` 产物相关逻辑：运行 `npm run build` 或 `npm run build:min`。
* 修改 Demo 并需要验证真实页面行为：先重新构建 SDK，再用 Demo 页面验证。

如果验证命令无法运行，应在最终说明中写明原因、风险和已完成的替代检查。

---

## 完成定义

一个任务完成前，应至少满足：

* 已完成用户明确要求的改动。
* 已检查 `git diff`，确认没有无关改动。
* 已运行与改动范围匹配的验证命令，或明确说明无法运行的原因。
* 若修改公开 API，已检查 `.d.ts`、测试、Demo、文档和 CHANGELOG 是否需要同步。
* 若修改 Demo，已确认示例仍以展示 SDK 用法为核心。
* 未经用户明确要求，不要自动 stage、commit、push 或创建 PR。

---

## 生成产物与第三方资源

* 不要手动修改 `node_modules/`。
* 不要手动修改 `release/*.zip`。
* 不要手动修改 `dist/*`，除非任务明确要求更新构建产物或发布包。
* 不要随意修改 samples 中的第三方库、bootstrap、adapter、wasm、tflite 等资源。
* 不要把构建临时目录、调试输出或本地配置误提交。
* `demo/config.js` 可能包含本地调试配置，修改前先确认任务需要。

---

## 遇到阻碍时

遇到阻碍时，应先收敛问题，不要扩大修改范围：

* 如果同一测试或构建命令连续失败 3 次，应停止继续猜测式修改，保留失败信息并说明原因。
* 如果缺少依赖，先检查 `package.json`、`npm-scripts.js`、`gulpfile.js`，不要直接引入新依赖。
* 如果遇到不相关的用户改动，不要覆盖、回滚或格式化这些改动。
* 如果遇到合并冲突、权限问题或环境问题，应说明受影响文件和当前状态。
* 绝对不要通过删除测试、跳过测试、删除源码、修改构建产物来掩盖问题。
* 绝对不要执行 `git reset --hard`、强制 push 或清理用户改动，除非用户明确要求。

---

## 编码规范

* 优先使用简单直接的代码，保证阅读顺序和执行流程清楚。
* 避免过度封装，避免多层包装导致逻辑难以追踪。
* 保留必要错误处理，避免大量无意义 try-catch。
* 修改资源释放、track 生命周期、timer、worker、AudioContext、canvas 等逻辑时，要检查是否会泄漏或提前释放。
* 新增复杂逻辑时，注释优先解释为什么这样做（Why），不要重复代码已经表达清楚的内容（What）。

---

## 日志规范

新增日志时应遵循：

* 记录关键状态变化。
* 记录降级原因。
* 记录失败原因。
* 不记录大量重复日志。
* 不打印 SDP 全文。
* 不打印 token、密码、鉴权头等敏感信息。
* 重要状态变化、降级原因和兼容分支应便于通过 logger 或事件排查。

---

## Commit 规范

生成 commit message 时，优先使用简洁明确的中文或英文，说明客户可感知或开发者可理解的核心变化。

推荐前缀：

* `feat:` 新增能力。
* `fix:` 修复问题。
* `refactor:` 重构内部实现，不改变公开行为。
* `docs:` 修改文档或 Demo 注释。
* `test:` 修改测试。
* `build:` 修改构建、打包或发布流程。
* `chore:` 其他维护性调整。

Commit message 不要夸大影响范围，不要把测试、格式化、重命名等内部细节写成用户可感知能力。

---

## CHANGELOG 规范

CHANGELOG 面向客户和接入方，不是内部开发记录。

编写 CHANGELOG 时应遵循：

* 只记录客户可以感知的重要变更。
* 优先记录功能新增、功能优化、兼容性提升、稳定性修复。
* 优先记录会影响客户接入、使用体验、通话效果、兼容性的内容。
* 语言应简洁明确，便于客户理解。
* 一条版本记录只保留最重要的几项内容，避免写得过细。
* 不要写测试调整、打包流程、脚本修改、代码重构等内部实现细节。
* 不要写路径大小写、变量调整、资源释放、日志优化等研发细节。
* 不要直接照搬 git commit，需整理为客户可读的发布说明。

如果某项改动客户无明显感知，或无法帮助客户理解版本价值，一般不要写入 CHANGELOG。

可按以下类别组织内容，但不强制照搬格式：

* Added
* Changed
* Fixed
* Improved
* Compatibility
* Deprecated

---

# Demo 项目规范

## Demo 的目标

Demo 的核心目标是向开发者展示 SDK 如何使用。Demo 不是正式产品。

所有实现都应围绕以下目标：

* 如何初始化 SDK。
* 如何配置 SDK。
* 如何调用 SDK API。
* 如何监听 SDK 事件。
* 如何释放 SDK 资源。
* 如何正确接入 SDK。

如果某段代码无法帮助开发者理解 SDK 的使用方式，应当简化或删除。

---

## Demo 架构规范

* 保持架构简单。
* 保持业务逻辑最小化。
* 避免复杂架构设计。
* 避免过度封装。
* 避免复杂状态管理。
* 避免产品级别的兜底逻辑和异常恢复机制。
* SDK 调用代码应集中管理，开发者应能快速找到初始化、配置、API 调用、事件监听、生命周期管理和资源释放。
* 不要把 SDK 调用隐藏在多层封装之后。

不要在 Demo 中实现产品级逻辑：

* 登录流程。
* 权限系统。
* 数据缓存。
* 自动重连策略。
* 网络状态管理。
* Redux、Vuex 等复杂状态管理。
* 产品级错误恢复。

推荐：

UI -> SDK API

不推荐：

UI -> Service -> Manager -> Adapter -> SDK API

---

## Demo 注释规范

Demo 承担一部分文档职责，所有 SDK API 示例都应包含必要注释。

新增或修改 SDK API 示例时，尽量说明：

* 功能用途。
* 参数含义。
* 参数类型。
* 参数限制范围。
* 默认值。
* 返回值。
* 回调事件。
* 常见错误。
* 使用场景。

示例代码应具备文档价值，让开发者能直接复制到客户项目中参考。

---

## Demo 示例代码规范

示例代码应满足：

* 展示推荐接入方式。
* 展示标准调用流程。
* 避免不必要复杂度。
* 具有教学价值。
* 保留必要错误处理，展示推荐的错误处理方式。
* 避免大量无意义 try-catch。
* 避免过度兜底逻辑影响 SDK 使用流程展示。

---

## 代码生成前检查

生成或修改代码前请自检：

* 是否存在过度设计？
* 是否存在过度抽象？
* 是否存在过度拆分文件？
* 是否存在无意义设计模式？
* 是否有更简单实现方案？
* 是否误改了构建产物、发布包或第三方资源？
* 是否影响公开 API、事件、`.d.ts`、文档或 Demo？
* 是否改变了默认值、事件顺序、Promise 时机、错误类型或返回结构？
* 开发者是否能快速理解 SDK 用法？
* SDK API 是否容易定位？
* Demo 参数限制是否有注释说明？
* 示例代码是否具备教学价值？
* 是否存在资源泄漏？
* 是否存在浏览器兼容性风险？
* 是否遗漏 `.d.ts`、测试、Demo、文档或 CHANGELOG？

如果存在以上问题，请优先简化实现方案。
