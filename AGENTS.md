# SDK 项目开发规范

这是一个SIP+WebRTC的SDK项目

## 核心原则

* 保持简单（KISS）
* 不为未来需求设计（YAGNI）
* 优先解决当前问题
* 避免过度设计
* 优先修改现有代码，而不是新增大量文件
* 优先保证代码可维护性和可调试性

---

## 架构规范

* 不要为了架构而架构
* 不要为了使用设计模式而使用设计模式
* 不要创建没有实际价值的抽象层
* 不要创建只有一个实现类的接口
* 不要创建无实际价值的 Manager、Factory、Adapter、Repository、Strategy 等模式
* 相同逻辑出现 3 次以上再考虑抽象
* 优先组合而不是抽象
* 保持调用链简单清晰
* 如果存在多个实现方案，优先选择最简单的方案

---

## 文件组织规范

* 优先减少文件数量
* 相关逻辑尽量放在一起
* 不要过早拆分文件
* 不要随意新增目录层级
* 优先修改现有模块，而不是新增模块
* 小型功能允许放在同一个文件中

---

## SDK 开发规范

* 优先保证 API 易理解
* 优先保证调用流程清晰
* 优先保证调试方便
* 优先保证代码可读性
* 合理关注运行性能
* 尽量减少依赖
* 不要引入无实际价值的设计模式
* SDK 的核心逻辑应该容易定位

---

## 编码规范

* 优先使用简单直接的代码
* 优先保证可读性
* 避免过度封装
* 避免多层包装导致逻辑难以追踪
* 重要逻辑不要隐藏在多层调用中
* 代码执行流程应该容易理解

---

# Demo 项目规范

## Demo 的目标

Demo 的核心目标是：

向开发者展示 SDK 如何使用。

Demo 不是正式产品。

所有实现都应围绕以下目标：

* 如何初始化 SDK
* 如何配置 SDK
* 如何调用 SDK API
* 如何监听 SDK 事件
* 如何释放 SDK 资源
* 如何正确接入 SDK

如果某段代码无法帮助开发者理解 SDK 的使用方式，应当简化或删除。

---

## Demo 架构规范

* 保持架构简单
* 保持业务逻辑最小化
* 避免复杂架构设计
* 避免过度封装
* 避免复杂状态管理
* 避免产品级别的兜底逻辑
* 避免产品级别的异常恢复机制

---

## SDK 调用规范

SDK 调用代码应集中管理。

开发者应能够快速找到：

* SDK 初始化
* SDK 配置
* SDK API 调用
* SDK 事件监听
* SDK 生命周期管理
* SDK 资源释放

不要把核心 SDK 调用分散到大量文件中。

---

## SDK 调用展示规范

不要把 SDK 调用隐藏在多层封装之后。

推荐：

UI → SDK API

不推荐：

UI → Service → Manager → Adapter → SDK API

开发者应能直接看到 SDK 的真实调用方式。

---

## 注释规范

所有 SDK API 示例都应包含必要注释。

至少说明：

* 功能用途
* 参数含义
* 参数类型
* 参数限制范围
* 默认值
* 返回值
* 回调事件
* 常见错误
* 使用场景

示例代码应具备文档价值。

---

## 示例代码规范

示例代码应满足：

* 可以直接复制到客户项目中使用
* 展示推荐接入方式
* 展示标准调用流程
* 避免不必要复杂度
* 具有教学价值

---

## 错误处理规范

* 保留必要错误处理
* 展示推荐的错误处理方式
* 避免大量无意义 try-catch
* 避免过度兜底逻辑
* 避免影响 SDK 使用流程展示

---

## 代码生成前检查

生成代码前请自检：

* 是否存在过度设计？
* 是否存在过度抽象？
* 是否存在过度拆分文件？
* 是否存在无意义设计模式？
* 是否有更简单实现方案？
* 开发者是否能快速理解 SDK 用法？
* SDK API 是否容易定位？
* 参数限制是否有注释说明？
* 示例代码是否具备教学价值？

如果存在以上问题，请优先简化实现方案。
